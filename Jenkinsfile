pipeline {
    agent any

    tools  {nodejs "node"}
    environment {

    PATH = "C:\\WINDOWS\\SYSTEM32"

}
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}