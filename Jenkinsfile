pipeline {
    agent none
    environment {

    PATH = "C:\\WINDOWS\\SYSTEM32"

}
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}